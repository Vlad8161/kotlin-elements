rootProject.name = 'kotlinelements'

include 'lib'
findProject(':lib')?.name = 'kotlinelements'
include 'compile-testing'

if (!System.env.JITPACK) {
    include 'test-annot-processor'
    include 'annotation-test'
}